name: Main

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 9 * * MON'

jobs:

  build-test-publish:
    uses: cyber-dojo-languages/reusable-actions-workflows/.github/workflows/build-image-per-arch.yml@main
    permissions:
      packages: write
      contents: read
